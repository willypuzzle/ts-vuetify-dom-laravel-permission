const STATE_ENABLED = 1;
const STATE_DISABLED = 0;

/**
 * Canonical permissions
 * */
const READ = 'read';
const CREATE = 'create';
const UPDATE = 'update';
const DELETE = 'delete';

export {
    STATE_DISABLED,
    STATE_ENABLED,

    /**
     * Canonical permissions
     * */
    READ,
    CREATE,
    UPDATE,
    DELETE
}